name: CI

on:
  pull_request:
    branches: [ main ]

jobs:
  build-docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Docker image (CPU)
        run: docker build -f Dockerfile.cpu -t road-signs:test .